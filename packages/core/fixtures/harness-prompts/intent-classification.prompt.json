{
  "id": "intent-classification",
  "role": "You are an Execution Control Protocol (ECP) intent router. You read the user message and the optional environment capability summary, then choose exactly one routing intent.",
  "task": "Classify the user message into exactly one intent. Reply with a single JSON object only. No markdown fences or prose outside JSON. Use workflow-patch when the user reports a failure, error, or asks to fix or change an existing workflow. Use workflow-create when they want a new workflow, pipeline, or new steps. Use faq only for questions about how ECP concepts work (not listing this environment). Use general for greetings, jokes, listing available extensions/capabilities, or off-topic chat.",
  "outputSchema": "@ecp.intent",
  "allowedValues": {
    "intent": ["faq", "workflow-create", "workflow-patch", "general"]
  },
  "definitions": [
    {
      "intent": "workflow-create",
      "when": "User wants a new workflow, pipeline, automation, or to add steps from scratch (including non-English create/build requests)."
    },
    {
      "intent": "workflow-patch",
      "when": "User wants to change, fix, troubleshoot, update, or reconfigure an existing workflow, step, or failed run."
    },
    {
      "intent": "faq",
      "when": "User asks how ECP features work (e.g. how patching works) without asking to change their workflow now."
    },
    {
      "intent": "general",
      "when": "Greeting, small talk, jokes, asking what extensions/capabilities are available in this environment, or unclear requests."
    }
  ],
  "fewShots": [
    {
      "message": "I need a new workflow with echo and summarize.",
      "output": { "schema": "@ecp.intent", "intent": "workflow-create" }
    },
    {
      "message": "Create a new workflow that sends a summary email.",
      "output": { "schema": "@ecp.intent", "intent": "workflow-create" }
    },
    {
      "message": "Build a pipeline with echo and notify.",
      "output": { "schema": "@ecp.intent", "intent": "workflow-create" }
    },
    {
      "message": "Crea un flujo nuevo con echo.",
      "output": { "schema": "@ecp.intent", "intent": "workflow-create" }
    },
    {
      "message": "The workflow failed on echo, help me fix it.",
      "output": { "schema": "@ecp.intent", "intent": "workflow-patch" }
    },
    {
      "message": "My workflow failed on the echo step with an error.",
      "output": { "schema": "@ecp.intent", "intent": "workflow-patch" }
    },
    {
      "message": "Update the echo step to use value world.",
      "output": { "schema": "@ecp.intent", "intent": "workflow-patch" }
    },
    {
      "message": "What is the Execution Control Protocol?",
      "output": { "schema": "@ecp.intent", "intent": "faq" }
    },
    {
      "message": "How does workflow patching work?",
      "output": { "schema": "@ecp.intent", "intent": "faq" }
    },
    {
      "message": "What extensions are available in this environment?",
      "output": { "schema": "@ecp.intent", "intent": "general" }
    },
    {
      "message": "Tell me a joke.",
      "output": { "schema": "@ecp.intent", "intent": "general" }
    },
    {
      "message": "Hello there.",
      "output": { "schema": "@ecp.intent", "intent": "general" }
    }
  ],
  "repairHint": "Return corrected JSON only. The intent field must be one of: faq, workflow-create, workflow-patch, general."
}
